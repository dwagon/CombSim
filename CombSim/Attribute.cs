// These are boolean flags so we aren't slinging strings around

namespace CombSim
{
    public enum Attribute
    {
        // Warlock specials
        AgonizingBlast,
        EldritchSpear,
        RepellingBlast,
    }
}